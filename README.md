# actually useful userscripts

- `apps/*.user.js` — standalone userscripts that can be installed directly from GitHub raw URLs.
- `packages/*` — scripts that need a build step via `vite-plugin-monkey`.

## development

```bash
vp install
vpr ready
```
